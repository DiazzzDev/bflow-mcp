{
  "family": "${ECS_MCP_TASK_FAMILY}",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",

  "executionRoleArn": "${ECS_MCP_EXECUTION_ROLE_ARN}",
  "taskRoleArn": "${ECS_MCP_TASK_ROLE_ARN}",

  "containerDefinitions": [
    {
      "name": "bflow-mcp",
      "image": "placeholder",
      "essential": true,

      "portMappings": [
        { "containerPort": 8081, "protocol": "tcp" }
      ],

      "environment": [
        { "name": "SPRING_PROFILES_ACTIVE", "value": "prod" },
        { "name": "COGNITO_ISSUER_URI", "value": "${COGNITO_ISSUER_URI}" },
        { "name": "COGNITO_HOSTED_UI_DOMAIN", "value": "${COGNITO_HOSTED_UI_DOMAIN}" },
        { "name": "COGNITO_APP_CLIENT_ID", "value": "${COGNITO_APP_CLIENT_ID}" },
        { "name": "BFLOW_API_BASE_URL", "value": "${BFLOW_API_BASE_URL}" },
        { "name": "BFLOW_MCP_PUBLIC_BASE_URL", "value": "${BFLOW_MCP_PUBLIC_BASE_URL}" },
        { "name": "BFLOW_MCP_PROXY_SIGNING_SECRET", "value": "${BFLOW_MCP_PROXY_SIGNING_SECRET}" },
        { "name": "BFLOW_MCP_PROXY_ALLOWED_REDIRECT_URIS", "value": "${BFLOW_MCP_PROXY_ALLOWED_REDIRECT_URIS}" }
      ],

      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "${CLOUDWATCH_MCP_LOG_GROUP}",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}